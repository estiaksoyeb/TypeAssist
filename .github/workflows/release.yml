name: Make release

permissions:
  contents: write

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  release-for-github:
    name: "Release for GitHub"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Setup Android NDK
        run: |
          echo "y" | sudo ${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager --install "ndk;27.0.12077973"

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'adopt'
          java-version: 17

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build release with Gradle
        run: ./gradlew assembleRelease --stacktrace

      - name: Get tag name
        if: startsWith(github.ref, 'refs/tags/')
        run: echo "VERSION_TAG=$(basename $GITHUB_REF)" >> $GITHUB_ENV

      - name: Verify build output
        run: |
          echo "=== All APKs found ==="
          find app/build/outputs/apk -name "*.apk" -type f
          for FLAVOR in full fdroid; do
            RELEASE_DIR="app/build/outputs/apk/${FLAVOR}/release"
            if [ ! -d "$RELEASE_DIR" ]; then
              echo "Missing expected release dir: $RELEASE_DIR"
              exit 1
            fi
            echo "--- $FLAVOR ---"
            ls -lah "$RELEASE_DIR"
          done

      - name: Sign APKs
        run: |
          set -e

          # Decode Keystore from Secret (BASE64)
          echo "${{ secrets.SIGNING_KEY_BASE64 }}" | base64 -d > release.keystore

          APKSIGNER="$ANDROID_HOME/build-tools/34.0.0/apksigner"
          CHECKSUM_FULL=""
          CHECKSUM_FDROID=""

          for FLAVOR in full fdroid; do
            RELEASE_DIR="app/build/outputs/apk/${FLAVOR}/release"
            apk_file="$RELEASE_DIR/app-${FLAVOR}-release-unsigned.apk"
            signed_apk="$RELEASE_DIR/TypeAssist-${FLAVOR}-${VERSION_TAG}.apk"

            if [ ! -f "$apk_file" ]; then
              echo "APK not found: $apk_file"
              ls -lah "$RELEASE_DIR"
              rm release.keystore
              exit 1
            fi

            $APKSIGNER sign \
              --ks release.keystore \
              --ks-key-alias "${{ secrets.ALIAS }}" \
              --ks-pass "pass:${{ secrets.KEY_STORE_PASSWORD }}" \
              --key-pass "pass:${{ secrets.KEY_PASSWORD }}" \
              --out "$signed_apk" \
              "$apk_file"

            $APKSIGNER verify "$signed_apk"
            ls -lh "$signed_apk"

            checksum=$(sha256sum "$signed_apk" | awk '{ print $1 }')
            if [ "$FLAVOR" = "full" ]; then
              CHECKSUM_FULL="$checksum"
            else
              CHECKSUM_FDROID="$checksum"
            fi
          done

          rm release.keystore

          echo "apk_sha256_full=$CHECKSUM_FULL" >> "$GITHUB_ENV"
          echo "apk_sha256_fdroid=$CHECKSUM_FDROID" >> "$GITHUB_ENV"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.VERSION_TAG }}
          name: TypeAssist ${{ env.VERSION_TAG }}
          body: |
            ---
            ### Checksums

            | Variant | SHA-256 |
            | ------- | ------- |
            | Full | ${{ env.apk_sha256_full }} |
            | F-Droid | ${{ env.apk_sha256_fdroid }} |
          files: |
            app/build/outputs/apk/full/release/TypeAssist-full-${{ env.VERSION_TAG }}.apk
            app/build/outputs/apk/fdroid/release/TypeAssist-fdroid-${{ env.VERSION_TAG }}.apk
          draft: true
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
